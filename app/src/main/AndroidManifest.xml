<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- Required by connectedDevice foreground service type on Android 14+ -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="androidx.car.app.NAVIGATION_TEMPLATES" />

    <!-- USB Host Feature (required) -->
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="true" />

    <!-- Optional Features -->
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.type.automotive"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarlinkNative"
        tools:targetApi="35">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.CarlinkNative.Fullscreen"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Separate CAR_MODE filter for automotive launchers -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.CAR_MODE" />
            </intent-filter>

            <!-- AAOS Media App Intent Filter -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.APP_MUSIC" />
            </intent-filter>

            <!-- USB Device Attachment -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>

            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/usb_device_filter" />

            <!-- AAOS Distraction Optimization for Activity -->
            <meta-data
                android:name="distractionOptimized"
                android:value="true" />
        </activity>

        <!-- Hidden CarAppActivity — exists solely to trigger Templates Host binding.
             NOT a launcher. Lives in separate task. Excluded from recents.
             MainActivity remains the primary UI. -->
        <activity
            android:name="androidx.car.app.activity.CarAppActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:taskAffinity="zeno.carlink.templates"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <meta-data
                android:name="distractionOptimized"
                android:value="true" />
        </activity>

        <!-- MediaBrowserService for AAOS Integration -->
        <service
            android:name=".media.CarlinkMediaBrowserService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback|connectedDevice"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
            <!-- GM Homescreen: Hide from launcher while keeping service functional -->
            <meta-data
                android:name="com.gm.homescreen.visibility"
                android:value="HIDDEN" />
        </service>

        <!-- Cluster Navigation CarAppService (headless — no UI on main display) -->
        <service
            android:name=".cluster.CarlinkClusterService"
            android:enabled="false"
            android:exported="true"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.NAVIGATION" />
                <category android:name="androidx.car.app.category.FEATURE_CLUSTER" />
            </intent-filter>
            <meta-data android:name="androidx.car.app.minCarApiLevel" android:value="6" />
            <meta-data android:name="com.gm.homescreen.visibility" android:value="HIDDEN" />
        </service>

        <meta-data
            android:name="com.android.automotive"
            android:resource="@xml/automotive_app_desc" />

        <!-- Required at application level — Templates Host reads minCarApiLevel
             from application metadata, not from service element -->
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="6" />

        <!-- Voice Interaction Service — registers as voice assistant for Siri PTT passthrough -->
        <service
            android:name=".service.voice.CarlinkVoiceInteractionService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:exported="true">
            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/voice_interaction_service" />
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>
        </service>

        <!-- Voice Interaction Session Service -->
        <service
            android:name=".service.voice.CarlinkVoiceInteractionSessionService"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:exported="true" />

        <!-- Recognition Service (stub — actual recognition handled by Siri via CarPlay) -->
        <service
            android:name=".service.voice.CarlinkRecognitionService"
            android:label="@string/app_name"
            android:exported="true"
            android:permission="android.permission.BIND_VOICE_INTERACTION">
            <intent-filter>
                <action android:name="android.speech.RecognitionService" />
            </intent-filter>
            <meta-data
                android:name="android.speech"
                android:resource="@xml/recognition_service" />
        </service>

        <!-- APPLICATION-LEVEL DISTRACTION OPTIMIZATION DECLARATION -->
        <meta-data
            android:name="com.android.automotive.application.DISTRACTION_OPTIMIZED"
            android:value="true" />

        <!-- ClusterIconContentProvider shim removed for Play Store compatibility.
             The authority is claimed by another developer globally. The shim only works
             when sideloading. NavigationStateManager detects its absence at runtime and
             disables AA bitmap icon delivery gracefully. -->

    </application>

</manifest>
